//! Reserved seat for the eIDAS qualified-seal step in `publish` — not wired
//! yet. `dpp-seal::QtspSealAdapter` exists but this service doesn't call it;
//! when it does, the seal call belongs here, alongside `publish.rs`.
