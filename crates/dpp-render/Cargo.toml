[package]
name        = "dpp-render"
version     = { workspace = true }
edition     = { workspace = true }
authors     = { workspace = true }
license     = { workspace = true }
repository  = { workspace = true }
description = "Shared HTML rendering of the public passport view"
publish = false

[dependencies]
dpp-digital-link = { workspace = true }

serde_json = { workspace = true }
qrcode     = { workspace = true }
uuid       = { workspace = true }
chrono     = { workspace = true }

[dev-dependencies]
# Test-only: binds the section fixtures to dpp-domain's serde contract so a
# field renamed in core fails a test instead of silently rendering a dash.
dpp-domain = { workspace = true }
