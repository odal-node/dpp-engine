//! CSC capability probing — not yet implemented.
//!
//! Reserved seat for validating a configured credential before trusting it:
//! checking `credentials/info`'s credential type/format against what the
//! adapter requires, and rejecting natural-person credentials (only
//! organisation/legal-entity seals are valid for a DPP operator).
