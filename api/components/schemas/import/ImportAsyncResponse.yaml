type: object
required:
  - jobId
  - status
  - totalRows
description: Returned for async imports (> 100 valid rows). Poll the job-status endpoint.
properties:
  jobId:
    type: string
    format: uuid
  status:
    type: string
    example: queued
  totalRows:
    type: integer
