x-tags:
  - Bulk Import
type: object
required:
  - row
  - field
  - message
properties:
  row:
    type: integer
  field:
    type: string
    description: Column name, or "vault" / "auth" / "internal".
  message:
    type: string
