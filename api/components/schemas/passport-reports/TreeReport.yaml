x-tags:
  - Passport Reports
type: object
required:
  - verified
  - nodes
description: Result of recursively verifying a passport's component tree.
properties:
  verified:
    type: boolean
    description: True iff every visited node verified.
  nodes:
    type: array
    items:
      $ref: ./TreeNodeReport.yaml
